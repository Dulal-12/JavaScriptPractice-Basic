var number = [1,2,3,4,5];
var newNumber = number.map((digit)=>digit*2); //update array digit but not using loop 
console.log(newNumber.map(value=>{
    
    //i can do anything here.
    // declare new function and anything with this value 
     return value*2})); //when you work in one line not use return
     //but work  with 1< more line you should use return keyword
     yy7 
// console.log(newNumber);
